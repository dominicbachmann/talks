import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="Popover API"
      subtitle="Native popovers with light-dismiss, top layer, and zero JavaScript"
      baseline="Baseline January 2025"
    />
  );
}
