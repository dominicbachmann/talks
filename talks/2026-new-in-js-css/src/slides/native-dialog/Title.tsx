import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="Native Dialog"
      subtitle={<>Modal &amp; non-modal dialogs with focus trapping, backdrop, and return values — built into the browser</>}
      baseline="Baseline since 2022"
    />
  );
}
