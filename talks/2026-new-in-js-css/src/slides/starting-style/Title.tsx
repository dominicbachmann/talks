import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title={<code>@starting-style</code>}
      subtitle={<>Animate entry from <code>display: none</code> — in pure CSS</>}
      baseline="Baseline 2024"
    />
  );
}
