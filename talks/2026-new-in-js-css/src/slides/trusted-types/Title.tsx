import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="Trusted Types"
      subtitle="Closing the DOM XSS gap that CSP can't reach"
      baseline="Baseline February 2026"
    />
  );
}
