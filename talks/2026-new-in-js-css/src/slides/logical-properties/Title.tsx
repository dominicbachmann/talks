import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="CSS Logical Properties"
      subtitle="Flow-relative layout that adapts to any writing direction — no separate RTL stylesheets, no overrides"
      baseline="Baseline since April 2021"
    />
  );
}
