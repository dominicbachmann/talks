import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="Invoker Commands"
      subtitle={<>Declarative buttons that open dialogs, toggle popovers, and dispatch custom actions — no <code>addEventListener</code> required</>}
      baseline="Baseline December 2025"
    />
  );
}
