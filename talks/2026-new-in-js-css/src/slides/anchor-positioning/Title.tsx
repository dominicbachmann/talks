import { SectionTitle } from "../../shared/SectionTitle";

export function Title() {
  return (
    <SectionTitle
      title="Anchor Positioning"
      subtitle="Position elements relative to each other — in pure CSS"
      baseline="Baseline January 2026"
    />
  );
}
